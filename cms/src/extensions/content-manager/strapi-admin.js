'use strict';

/**Within this file, define and export a function.
 * The function receives the plugin interface as an argument so it can be extended. */

module.exports = (plugin) => {
  console.log("HEJ FRÅN PLUGIN", plugin);

  return plugin
}


